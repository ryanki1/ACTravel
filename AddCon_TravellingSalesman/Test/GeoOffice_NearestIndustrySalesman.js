module("GeoOffice can rank salesmen by industry knowledge", {
    setup: function () {
        localStorage.clear();
        app.reset();
    },
    teardown: function () {
    }
});

test("As already discussed list should show salesman in terms of amount of industry experience, most first", function () {
    ok(true === true, "Success");
})

test("As already discussed list should show industry experience in picture form", function () {
    ok(true === true, "Success");
})

test("As already discussed list clicking should show salesman details in terms of industry experience", function () {
    ok(true === true, "Success");
})

test("As already discussed list rotating to portrait view should show currently showing industries in terms of distance away from user", function () {
    ok(true === true, "Success");
})

test("As already discussed portrait view should be entitled salesman by industry", function () {
    ok(true === true, "Success");
})